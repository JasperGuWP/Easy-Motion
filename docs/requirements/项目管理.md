# 项目管理

## 项目窗口
- 独立窗口，用于浏览、创建、打开、删除项目
- 显示项目列表（缩略图、名称、创建时间、修改时间）
- 支持搜索和筛选

## 项目层级结构
### 总项目（视频期）
- 代表一期完整的视频内容
- 包含多个子项目（动画片段）
- 总项目配置文件记录整体分辨率、帧率、时长等

### 子项目（动画片段）
- 每个子项目是独立的动画片段
- 子项目独立文件夹，避免混乱
- 子项目可单独预览、渲染、导出
- 总项目时间线可引用多个子项目进行组合

## 总项目结构
```
总项目文件夹/
├── project.json              # 总项目配置（分辨率、帧率、子项目列表等）
├── subprojects/              # 子项目目录
│   ├── 动画片段1/
│   │   ├── remotion/         # 完整的独立 Remotion 项目
│   │   │   ├── node_modules/ # npm 依赖（独立安装，版本隔离）
│   │   │   ├── src/          # 源代码（Root.tsx, 组件等）
│   │   │   ├── package.json  # 依赖配置和脚本
│   │   │   ├── remotion.config.ts # Remotion 配置
│   │   │   ├── tsconfig.json # TypeScript 配置
│   │   │   └── ...
│   │   ├── assets/           # 子项目素材
│   │   ├── output/           # 子项目渲染输出
│   │   ├── data/             # 数据文件
│   │   └── subproject.json   # 子项目配置
│   └── 动画片段2/
│       └── ...
├── assets/                   # 总项目共享素材
├── output/                   # 总项目最终输出（合并后的视频）
└── data/                     # 总项目共享数据文件
```

## 子项目隔离优势
- **完全隔离**：每个子项目的依赖互不干扰，避免版本冲突
- **版本锁定**：可针对不同子项目使用不同 Remotion 版本（默认统一）
- **导出方便**：直接导出 `remotion/` 文件夹，对方 `npm install` 即可独立运行

## 项目隔离
- 所有操作限定在项目文件夹内
- 素材自动复制到项目 `assets/` 目录，避免外部依赖
- 删除项目时可选择是否保留输出文件

## 项目模板
- 内置基础 Remotion 模板
- 新建总项目时自动创建模板结构
- 新建子项目时复制 Remotion 模板到子项目目录
- 支持用户自定义模板

## 版本管理
- 自动保存项目状态到 `project.json` 和 `subproject.json`
- 支持手动创建快照/版本
- 支持回滚到历史版本
- 子项目版本独立管理

---

*文档版本：v0.1 | 最后更新：2026-05-29*
