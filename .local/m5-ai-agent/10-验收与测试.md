# 10 — 验收与测试

> 文档同步：2026-06-14。**M5 验收完成**

## M5 完成定义

- [x] **LangChain Agent** 调用 8 Tool（`agent/tools/index.js`）
- [x] IPC 对齐 conversation send/cancel/clear + load/save
- [x] `conversationStore` + `eventBus`（`diffReady` emit → `timelineStore` 订阅）
- [x] `langchain`、`@langchain/openai` 已加入 `apps/electron`
- [x] 无 `.env` Key 进库
- [x] A1–A6 手测签字

---

## 验收标准

| ID | 场景 | 实现 | 签字 |
|----|------|------|------|
| A1 | 创建 Hello 标题 | ✅ | ✅ |
| A2 | 字体大一点 | ✅ | ✅ |
| A3 | 持久化 | ✅ | ✅ |
| A4 | 降级 | ✅ | ✅ |
| A5 | 安全 | ✅ | ✅ |
| A6 | 多模态 | ✅ | ✅ |

---

## 测试分层

### 单元测试（不依赖 LLM）— `pnpm test:m5`

| 脚本 | 模块 |
|------|------|
| `test-conversation-service.js` | load/save/undo、E2701 |
| `test-conversation-errors.js` | E2700 |
| `test-agent-mock-ipc.js` | mock runAgent → IPC complete |
| `test-agent-prompt-regression.js` | Prompt/8 Tool/相对调整 |
| `test-llm-factory.js` | LangChain 冒烟、E2804 |
| `test-timeline-ops.js` | Tool 原语、冲突 |
| `test-clip-updates.js` | 相对调整 |
| `test-agent-resilience.js` | security-scan、fallback |
| `test-vision-layout.js` | ai-refs、layout-mapper |
| `test-asset-import.js` | importAsset |

### 待补充

- [x] E2701 损坏 `conversation.json` 单测
- [x] Mock `runAgent` → IPC 集成测试（`test-agent-mock-ipc.js`）
- [x] Prompt 回归静态断言（`test-agent-prompt-regression.js`）
- [x] A1–A6 手测签字（2026-06-14）

### 手动 E2E

- `node scripts/test-agent-live.js`（需 API Key）
- 路线图 §M5 六条场景

---

## 组件 UI 回归

- [x] `GenerationProgress` 随 `agentStatus` 变化
- [x] `MessageItem` 支持 system 类型
- [x] `ImageAttachment` 历史消息缩略图（`MessageImageThumbnails`）
- [x] 参考图排序 UI（`useImageReorder`）

---

## 性能（非阻断）

| 指标 | 目标 |
|------|------|
| LangChain 首 token | < 5s |
| A1 端到端 | < 30s |
| conversation save | < 200ms |
