# 10 — 验收与测试

## M5 完成定义

- [ ] A1–A5 通过；A6 通过或标 M5.1
- [ ] **LangChain Agent** 调用 8 Tool（至少 create/modify 路径验证）
- [ ] IPC 对齐应用架构 conversation + IPC规范 load/save
- [ ] `conversationStore` + `eventBus` 对齐状态管理详细设计
- [ ] 依赖清单中的 `langchain`、`@langchain/openai` 已加入 `apps/electron`
- [ ] 无 `.env` Key 进库

---

## 验收标准

| ID | 场景 | 通过标准 |
|----|------|----------|
| A1 | 创建 Hello 标题 | LangChain Tool 改 timeline + 预览 |
| A2 | 字体大一点 | `updateClip` Tool + 预览 |
| A3 | 持久化 | `conversation.json` 子项目隔离 |
| A4 | 降级 | 重试 + 简化模式 |
| A5 | 安全 | 违规 TSX 拒绝 |
| A6 | 多模态 | 视觉 Prompt + Tool 布局 |

---

## 测试分层

### 单元测试（不依赖 LLM）

| 模块 | 用例 |
|------|------|
| `agent/tools/*` | 8 Tool 入参/出参 |
| `agent/prompts/system` | 上下文占位符替换 |
| `agent/fallback-templates` | 简化模式 |
| `conversation-service` | load/save 往返 |

### 集成测试

- Mock `ChatModel` → AgentExecutor 调用 `createTrack` mock
- Mock stream → `conversation:chunk` IPC

### LangChain 冒烟（Phase 2.5）

- 主进程单轮 `invoke`，有效 Key 返回非空

### 手动 E2E

路线图 §M5 六条 + [LLM-Agent设计.md](../../docs/requirements/LLM-Agent设计.md) Prompt 回归 10 条。

---

## 组件 UI 回归（组件库清单）

- [ ] `GenerationProgress` 随 `agentStatus` 变化
- [ ] `MessageItem` 支持 system 类型
- [ ] `ImageAttachment` 缩略图（Phase 6）

---

## 性能（非阻断）

| 指标 | 目标 |
|------|------|
| LangChain 首 token | < 5s |
| A1 端到端 | < 30s |
| conversation save | < 200ms |
