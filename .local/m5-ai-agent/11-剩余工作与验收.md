# 11 — 剩余工作与验收签字

> 文档同步：2026-06-14。**M5 代码 + 手测验收已完成**；M5.1 见下方延后项。

## 验收标准签字（A1–A6）

| ID | 场景 | 实现 | 签字 |
|----|------|------|------|
| A1 | 创建 Hello 标题 | ✅ Agent + 8 Tool + 预览 | ✅ 手测通过 |
| A2 | 字体大一点 | ✅ `updateClip` + 相对调整 | ✅ 手测通过 |
| A3 | `conversation.json` 持久化 | ✅ load/save/clear | ✅ 手测通过 |
| A4 | 超时重试 + 简化模式 | ✅ `stream-timeout` + `fallback-templates` | ✅ 手测通过 |
| A5 | TSX 安全违规拒绝 | ✅ `security-scan.js`（Generator 路径） | ✅ 手测通过 |
| A6 | 参考图 + 布局 | ✅ 视觉链 | ✅ 手测通过 |

---

## P1 — 规范对齐（小改）

| # | 任务 | 说明 |
|---|------|------|
| 1 | **E2700** | ✅ `conversation-errors.js` + `agent-service` + UI Toast |
| 2 | **`timelineStore` 订阅 `diffReady`** | ✅ `subscribeToEventBus` + `applyAgentTimelineDiff` |
| 3 | **锁定 `zod` 版本** | ✅ `4.4.3`（无 `^`） |
| 4 | **`intent.js`** | ✅ 不单独实现；意图解析在 `prompts/system.js` + Agent |

---

## P2 — UI / 体验（路线图有）

| # | 任务 | 说明 |
|---|------|------|
| 5 | **历史消息图片缩略图** | ✅ `MessageImageThumbnails` + `MessageItem` |
| 6 | **参考图拖拽排序** | ✅ `useImageReorder` + `ImageAttachment` |
| 7 | **对话面板收起/展开** | ⏭️ **不做** — 右侧 Tab（属性/素材/AI）已满足切换；`BottomConversationBar` 亦在 v1 范围外 |
| 8 | **子项目切换 UI** | ⏸️ **M5.1** — 当前仅 `subprojects/default`，后端已按 path 隔离 |

---

## P3 — 测试与工程

| # | 任务 | 说明 |
|---|------|------|
| 9 | **E2701 损坏文件单测** | ✅ `test-conversation-service.js` |
| 10 | **Mock ChatModel 集成测试** | ✅ `test-agent-mock-ipc.js`（mock `runAgent` → IPC complete） |
| 11 | **Prompt 回归 10 条** | ✅ `test-agent-prompt-regression.js`（Prompt/工具/相对调整静态断言） |
| 12 | **工具链中途失败回滚** | ⏸️ **M5.1** — 现有「撤销此次 AI 修改」；无多步原子回滚 |

---

## 已知实现差异（不必改，文档已记录）

| 文档写法 | 实际实现 |
|----------|----------|
| `createToolCallingAgent` + `AgentExecutor` | LangChain 1.4 `createAgent()`（`graph.js`） |
| 8 个独立 `tools/*.js` | 集中在 `agent/tools/index.js` |
| `intent.js` | 未单独文件 |
| `llm-service.ts` | `llm-service.js` |

---

## 执行计划（2026-06-14 起）

> **明确不做**：P2-7 对话面板折叠（右侧 Tab 已覆盖）。

| 阶段 | 项 | 动作 | 状态 |
|------|-----|------|------|
| **A 规范** | P1-3 | `zod` 锁定为 `4.4.3`（去掉 `^`） | ✅ |
| **A 规范** | P1-4 | `intent.js` 不新建文件；文档标为「合入 system.js」 | ✅ |
| **B 测试** | P3-9 | `test-conversation-service` 补 E2701 用例 | ✅ |
| **B 测试** | P3-10 | `test-agent-mock-ipc.js`：mock `runAgent` → IPC complete | ✅ |
| **B 测试** | P3-11 | `test-agent-prompt-regression.js`：Prompt/工具链静态断言 | ✅ |
| **C 延后** | P2-8 | 子项目切换 UI（仅 default 时不需要） | ⏸️ M5.1 |
| **C 延后** | P3-12 | 多步 Tool 原子回滚 | ⏸️ M5.1 |
| **D 手测** | A1–A6 | E2E 签字表 | ✅ |

每完成 A/B 一项 → 本文件打勾 → `pnpm test:m5`。

---

## M5.1 候选（非阻断）

- P2-8 子项目切换 UI
- P3-12 多步 Tool 原子回滚
- A6 布局准确度持续优化（可选）

